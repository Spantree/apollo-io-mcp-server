{
  "mcpServers": {
    "apollo-io": {
      "command": "uv",
      "args": [
        "run",
        "mcp",
        "run",
        "server.py"
      ],
      "env": {
        "APOLLO_IO_API_KEY": "${env:APOLLO_IO_API_KEY}"
      }
    }
  }
}
